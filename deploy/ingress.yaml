apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: neolesk
  namespace: kroki
  annotations:
    tailscale.com/hostname: neolesk
spec:
  defaultBackend:
    service:
      name: neolesk
      port:
        number: 80
  ingressClassName: tailscale
  tls:
    - hosts:
        - neolesk
