apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: neolesk

build:
  artifacts:
    - image: darren/neolesk
      context: .
      docker:
        dockerfile: Dockerfile
  local:
    push: true

manifests:
  kustomize:
    paths:
      - deploy
    buildArgs:
      - --enable-helm
